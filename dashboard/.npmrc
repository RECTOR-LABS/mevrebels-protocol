# Allow installation to proceed even if optional dependencies fail
optional=true

# Continue on error for native modules (usb package from Trezor hardware wallet)
# Hardware wallet support is optional for MVP
foreground-scripts=true